# Spec: Manual Benchmark Coalescing during Staging in Prism

- **Status**: Draft
- **Author**: diamondburned, Jetski
- **Date**: August 3, 2026
- **Feature Name**: `manual-benchmark-coalescing`

## Objective & Problem Statement

This specification defines the product, UI/UX, and technical design for **manual benchmark coalescing, auto-grouping, and stage re-ordering during staging in Prism**.

In the Prism Results Store, stage reports generated by upstream benchmark tools are parsed and automatically grouped into benchmark runs during file upload or scanning. The exact rules governing this grouping mechanism are specified in the [Parser & Data Model Specification](file:///usr/local/google/home/diamondburned/Projects/llm-d/llm-d-prism/specs/main/completed/results-api/parser_and_data_model.md#L96-L117):
1. **Explicit Run ID Matching**: Files containing identical `run.uid` values are grouped together.
2. **Directory-Based Grouping**: Files uploaded from the exact same parent directory are coalesced into a single run.
3. **Metadata Equivalence Fallback**: Stages sharing identical `scenario.load.metadata` (verified via `canonicalStringify`) are coalesced.

### The Problem: Disjointed Harness Invocations (e.g., uBench)

For benchmarking tools such as **`uBench`**, multi-stage benchmarks are executed as separate, individual `inference-perf` harness calls rather than a single multi-stage `inference-perf` invocation. As a result:
- The generated **Benchmark Report v0.2 (BRV0.2)** outputs have **no `stage` references** or stage indices.
- Stage files are output across different subdirectories or files without matching `run.uid` fields.
- Automated parsing fails to group these stages together, leaving them split into multiple separate benchmark run payloads in the local staging workspace ([SubmitValidationPage.jsx](file:///usr/local/google/home/diamondburned/Projects/llm-d/llm-d-prism/src/components/DataConnections/SubmitValidationPage.jsx)).

To solve this issue, users must be able to **manually group (coalesce) separate benchmarks into a single run** directly within the Prism UI, resolve any metadata conflicts inline, run heuristic **Auto-Group** operations, and **ungroup (split)** coalesced runs when necessary.

## User Story & Background Context

From Gemini summary of the Prism Dashboard Walkthrough meeting with the uBench team:

> **Multi-stage functionality future support:** The team acknowledges the need to support multi-stage functionality and agrees to develop a plan to enable the consolidation of separate points into a single batch line.
>
> **Multi-stage Benchmark Visualization:** diamond demonstrated how Prism uses a shared config ID to automatically combine multiple stages from a benchmark run into a single line (00:15:24). Joey and diamond discussed the potential to implement functionality that allows users to manually consolidate separate benchmarks into a single run, which is a desired enhancement (00:18:49).

## Key Requirements & Scope

1. **Manual Benchmark Coalescing**: Select $\ge 2$ staged benchmark runs and merge them into a single consolidated benchmark payload.
2. **Interactive Conflict Resolution UI**:
   - For every mismatched metadata field, present radio choices for each distinct value found across the selected runs.
   - Provide a **"Custom value..."** option as the final choice, revealing an inline text input bar for custom entries.
   - Provide preset shortcuts (*"Apply All from Run #1"*, *"Apply All from Run #2"*).
3. **Heuristic "Auto-Group"**: A single-click action that scans all staged benchmarks and automatically groups runs matching on `model_name`, `hardware_name`, serving stack, and execution timestamp proximity.
4. **Ungroup / Undo Support**: Any coalesced or auto-grouped benchmark run displays an **"Ungroup Benchmarks"** (Undo) action, allowing users to split a grouped run back into its original constituent source reports before final submission.
5. **Normalized & Drag-and-Drop Sortable Stages**:
   - All constituent stage runs are **fully preserved without deletion**; duplicate or non-standard stage numbers (e.g. `0, 0, 1, 2, 3`) are re-indexed and normalized sequentially into `0, 1, 2, 3, 4, ...`.
   - Stages in the coalesced run preview/editor are **drag-and-drop sortable**. Dragging a stage reorders the list, and stages immediately adopt their new sequential index (`0, 1, 2, 3, 4, ...`).
6. **Raw Report Immutability & Prism Payload Separation**:
   - The raw BRV0.2 report object inside `entries[].raw_report` (including any original `stage: n` field in BRV0.2) remains **completely untouched and unmodified**.
   - Normalized stage ordering is stored exclusively in the **Prism upload payload structure** via a dedicated `prism_stage_index` property on each `PrismStageEntry`.
