# Agent Rules

## Git Commits
- Whenever you (the AI agent) create a git commit in this repository, you MUST always include the `-s` (or `--signoff`) flag to ensure Developer Certificate of Origin (DCO) sign-off.
- Commits are automatically signed using SSH key-signing, which is configured globally on the user's system.
